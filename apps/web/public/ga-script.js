// GA script to be loaded conditionally based on cookie consent
window.dataLayer = window.dataLayer || [];
function gtag() { dataLayer.push(arguments); }
gtag('js', new Date());

// The config call will be made when cookie consent is given
// This allows the script to be loaded without configuring tracking immediately 